export interface Iporder {
     objectId:number, //{ get; set; }
     corte:string, //{ get; set; }
     cantidad:number, //{ get; set; }
     estilo:string, // { get; set; }
     washed:boolean //{ get; set; }
}
